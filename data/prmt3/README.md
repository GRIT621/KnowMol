# PRMT3 Candidate Set

`prmt3_candidates_20.csv` contains the 20 candidate ligands used by
`/data/lsj/KnowMol/EviDTI_dataset/prtm3/train_for_ptrm3.py`.

The original script also includes an internal `"aaa"` entry before these
candidates; it is not included here.
